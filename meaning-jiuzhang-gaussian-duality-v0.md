# 九章高斯对偶映射 v0（Jiuzhang-Gaussian duality mapping v0）

日期：2026-09-10。状态：`proposed-document`（proposed-mapping，待人类评审；
散文不被验证为真）。方向：Mingli Yuan；整理：assistant。
目录学条目：`logic-jiuzhang-gaussian-duality`（home=logic）。

## 1. 历史对象

《九章算术》（先秦–汉代）"方程"章：算筹在算板上摆出矩形数字网格，
发明"正负术"（红筹负、黑筹正）与"遍乘直除法"；其消元法在数学实质上
即增广矩阵的初等行变换（高斯消元），是数学史上最早的矩阵雏形。
本条目只记录**对位**，不做历史训诂。

## 2. 对位（每重指认我们的材料）

1. **载体对偶**：算筹矩形网格（位置承载数值）= neutral carrier /
   PSC0 载体 / ι/SKI 基底。意义在位置不在符号。
2. **正负对偶**：正负术（红负黑正）= 有符号算术：正行/逆行 ±1/±2
   （receipt-15/16）、M0/M1/M2 符号标记、双观察者 i−j 约束。
   负数之发明为消元服务——操作倒逼表示。
3. **遍乘对偶**：遍乘（整行乘）= 初等行变换①行缩放 =
   normalize-before-eval、byte budget 缩放。
4. **直除对偶**：直除（行间减倍数）= 初等行变换③行倍加 =
   收据账本的三处声明修复（receipt-02/03/05）。repair 即行变换。
5. **换行对偶**：换行 = 初等行变换② = symbol-surface swap.py 端点交换
   + 反向学习 switch swap。
6. **消元对偶**：消元 = ledger_check 前向消元（逐行校验前驱 pin 为
   pivot）；收据链是上三角矩阵。
7. **回代对偶**：reverse-receipt-trace 的逆行解读 = 回代
   （back-substitution，沿前驱走回起点）。
8. **成对对偶**：与花拉子米（条目 18）成对：单一未知 → n 个未知；
   面积 → 网格；六种标准型 → 一个通用算法；矩阵突破几何的维度上限。

## 3. 边界与义务

- 用户描述称"算筹在算盘上"；通行叙述为算筹在**算板**上（算盘为后世
  工具）——照实标记，不静默改正。
- 对位是解释，不是同构：未声称收据账本"就是"矩阵（但它按矩阵方式
  被检查）。
- 采用通行数学史归纳；未核对郭书春校注本原文。
- 本条目不引入任何原生语义、目录权限或检查器变更。
