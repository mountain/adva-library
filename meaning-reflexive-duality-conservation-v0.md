# 反射对偶守恒映射 v0（reflexive duality conservation mapping v0）

日期：2026-09-10。状态：`proposed-document`（proposed-mapping，待人类评审；
散文不被验证为真）。方向：Mingli Yuan；整理：assistant。
目录学条目：`logic-reflexive-duality-conservation`（home=logic）。

## 1. 对象

会话中的 (+, −, −) 结构,其精确实例为立方根三元组 {1, ω, ω²}
（scale-marked surface 的 M0/M1/M2 标记，ε = e^{2πi/3} = ω）：
实数部分恰为 (+, −, −)。本条目只记录**对位**，不做物理主张。

## 2. 对位（每重指认我们的材料）

1. **吸引对偶**：两个负号趋于正号、对偶抵消归零 = 1 + ω + ω² = 0
   = al-muqābala 对消（条目 18）+ Burau 边界对闭合（receipt-21/22）
   + EvidenceStutter + 收据链修复（receipt-02/03/05）。
2. **斥力对偶**：负负相遇沿边界碰撞、镜像反射 = 门的拒读 0/25
   （round-22 证伪测试）+ Burau 两个负论文在 n=5 边界镜像分明
   + EvidenceStutter/VariationObserved 互不融合。
3. **守恒对偶**：总量恒一 = det(L)·det(L⁻¹) = (1/3)·3 = 1 +
   能量积 1/9×9 = 1 + 交换协议 resource_grant = 0（每轮）+
   LedgerConsistent。
4. **号差对偶**：(+, −, −) = Minkowski 号差：正方向时间向（吸引，
   测地线汇聚），负方向空间向（边界镜像反射）。
5. **反射对偶**：ω ↔ ω² 共轭镜像 = 反射格点多面体的极对偶
   P ↔ P*（对面 PR #175 的 lattice-polar 有界配对运输）；反射性
   即对偶在格点世界双向闭合——隔阂可计算的条件。
6. **求一对偶**：守恒的单位 1 = 秦九韶求一术要找的那个 1
   （条目 20）。吸引释放差（归零），斥力保存区别（成对），
   总量恒一。

## 3. 边界与义务

- {1, ω, ω²} 与 (+, −, −) 号差的对应取实数部分，是解释层对应，
  非数学同构。
- 时空读法是类比，不是物理主张。
- 未声称存在单一守恒算符；守恒体现为协议不变量（零授予、det 积、
  链一致）。
- 本条目不引入任何原生语义、目录权限或检查器变更。
